/**
 * Shared Prettier configuration for the ProjectSim 2.0 monorepo.
 * @type {import("prettier").Config}
 */
const config = {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 80,
  tabWidth: 2,
  endOfLine: "lf",
};

export default config;
