{
  "name": "@projectsim/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared build, lint, and formatting configuration for the ProjectSim 2.0 monorepo.",
  "type": "module",
  "exports": {
    "./tsconfig.base.json": "./tsconfig.base.json",
    "./eslint": "./eslint-preset.mjs",
    "./prettier": "./prettier-preset.mjs"
  },
  "files": [
    "tsconfig.base.json",
    "eslint-preset.mjs",
    "prettier-preset.mjs"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "echo \"@projectsim/config: no TypeScript sources to typecheck\"",
    "test": "echo \"@projectsim/config: no tests\""
  }
}
