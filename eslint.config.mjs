import baseConfig from "@projectsim/config/eslint";

/**
 * Root ESLint flat config for the ProjectSim 2.0 monorepo.
 * The shared preset lives in `@projectsim/config` so every workspace
 * inherits identical rules and dependency-boundary expectations.
 */
export default baseConfig;
